# Supabase
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# OpenAI (proxy this through a backend in production — never ship raw keys)
EXPO_PUBLIC_OPENAI_API_KEY=

# Force mock data path when keys are absent
EXPO_PUBLIC_USE_MOCK_DATA=true
