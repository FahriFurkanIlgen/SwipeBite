import { Recipe } from "@/types/domain";

/**
 * Previously imported from TheMealDB. Disabled — most recipes contain
 * pork / non-halal ingredients, not suitable for Turkish households.
 *
 * Kept as empty export so existing imports keep compiling. Use
 * scripts/scrape-yemekcom.ts instead.
 */
export const THEMEALDB_RECIPES: Recipe[] = [];
